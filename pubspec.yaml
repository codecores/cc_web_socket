name: cc_web_socket
description: "A new Flutter project."
version: 1.0.0+1
homepage: https://github.com/codecores/cc_web_socket

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  web_socket_channel: ^2.4.0
  synchronized: ^3.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true